fastapi==0.115.0
uvicorn[standard]==0.30.6
sqlalchemy==2.0.35
psycopg[binary]==3.2.2
alembic==1.13.2
pydantic-settings==2.5.2
PyJWT[crypto]==2.9.0
httpx==0.27.2
# Mã hoá Host.ssh_password_encrypted (xem app/hosts.py) — vốn đã có sẵn qua
# PyJWT[crypto] (transitive), khai rõ ràng ở đây để không phụ thuộc ngầm vào
# extras của 1 package khác cho 1 nhu cầu bảo mật riêng.
cryptography==49.0.0
# Parse file playbook Ansible chính thức (ComplianceAsCode) cho tab
# "Template" (xem app/control_templates.py) — vốn đã có sẵn qua transitive
# dependency (Ansible-adjacent tooling), khai rõ ràng để không phụ thuộc ngầm.
PyYAML==6.0.3

# dev/test
pytest==8.3.3
